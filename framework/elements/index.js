const Button = require('./button');
const Input = require('./input');
const Paragraph = require('./paragraph');
const Textarea = require('./textarea');

module.exports = { Button, Input, Paragraph, Textarea };
